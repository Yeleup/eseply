# Техническое задание

Система учёта коммунальных услуг

0. Терминология

В документации и коде используются следующие соответствия:

| Бизнес-термин | Техническое имя |
|---|---|
| Tenant-организация | `Organization` |
| Абонент коммунальных услуг | `Client` |
| Лицевой счёт | поле клиента `account_number` |
| Услуга организации | `UtilityService` / основная коммунальная услуга tenant-организации |
| Тип абонента | `ClientType`, поле клиента `client_type` |
| Регион | `Region` |
| Улица | `Street` |
| Счётчик | `Meter` |
| Показание счётчика | `MeterReading` |
| Оплата | `Payment` |
| Квитанция | `Receipt` |
| Услуга клиента | услуга выбранной tenant-организации, применяемая к абоненту |

Термин “абонент” в пользовательском интерфейсе и бизнес-правилах соответствует сущности `Client`.
Лицевой счёт хранится как поле клиента, например `account_number`.

1. Назначение системы
Необходимо разработать систему для учёта коммунальных услуг.
Система должна позволять вести абонентов, услугу организации, типы абонентов, тарифы, счётчики, показания, оплаты, начисления, сальдо, закрытие месяца, перерасчёты и квитанции.
Система должна поддерживать разные виды коммунальных услуг:


водоснабжение;


электроэнергия;


вывоз мусора;


канализация;


обслуживание;


другие услуги при необходимости.


Tenant-организация всегда ведёт учёт только по одной коммунальной услуге.
Например, отдельная организация создаётся для водоснабжения, отдельная организация — для вывоза мусора.
Если нужно вести новую услугу, оператор создаёт новую tenant-организацию под эту услугу.
Внутри одной организации нельзя добавить несколько услуг и нельзя выбирать разные услуги для разных абонентов.

2. Основной принцип системы
Расчёты ведутся по связке:
Лицевой счёт + Период
Услуга берётся из выбранной tenant-организации.
Тип начисления указывается в карточке абонента.
Внутри одной организации все абоненты обслуживаются по одной коммунальной услуге.
Пример:
ОрганизацияУслугаЛицевой счётАбонентТип начисленияТОО “Водоканал”Водоснабжение10001Иванов И.И.По счётчикуТОО “Водоканал”Водоснабжение10002Петров П.П.На одного человекаТОО “Тазалык”Вывоз мусора20001ТОО “Асыл”Фиксированная суммаТОО “Энерго”Электроэнергия30001Сидоров С.С.По счётчику

3. Основные возможности системы
Система должна позволять:


вести список абонентов;


вести лицевые счета;


использовать услугу выбранной организации для абонентов;


выбирать тип начисления для абонента;


задавать услугу tenant-организации;


вести тарифы;


вести справочник регионов и улиц внутри организации;


использовать типы абонентов для выбора тарифа;


учитывать счётчики;


вводить показания;


учитывать оплаты;


рассчитывать начисления;


рассчитывать начальное и конечное сальдо;


закрывать расчётный месяц;


выполнять перерасчёт;


формировать квитанции;


печатать квитанции;


просматривать историю начислений, оплат, показаний и перерасчётов;


формировать отчёты.



4. Организация
В системе должны храниться данные организации, от имени которой ведётся учёт.
У каждой tenant-организации должна быть одна основная коммунальная услуга.
Внутри организации не ведётся набор услуг.
Если нужна новая услуга, создаётся отдельная tenant-организация под эту услугу.
Тип начисления не относится к организации.
Тип начисления указывается только в карточке абонента.
Основные поля организации
ПолеОписаниеНазвание организацииПолное название организацииУслуга организацииВодоснабжение / электроэнергия / вывоз мусора / другая коммунальная услугаБИН / ИИНРеквизиты организацииТелефонКонтактный телефонАдресАдрес организацииБанкНазвание банкаIBANРасчётный счётПримечаниеДополнительная информация

Правила организации


организация используется как tenant-область административной панели;


организация имеет ровно одну основную коммунальную услугу;


услуга организации обязательна при настройке организации;


внутри одной организации нельзя добавить вторую коммунальную услугу;


для новой коммунальной услуги создаётся новая tenant-организация;


пользователь может быть участником нескольких организаций;


пользователь видит только организации, к которым он привязан;


при создании организации текущий пользователь становится её участником;


данные учёта должны относиться к выбранной организации, если соответствующий модуль поддерживает tenant-изоляцию.


абоненты, тарифы, счётчики, начисления и квитанции используют услугу выбранной организации.

Справочник адресов организации

В профиле организации должны быть доступны регионы как связанный справочник.
Регион в коде называется Region.
Таблица регионов называется regions.
Основные поля региона: id, organization_id, name.
Регион принадлежит tenant-организации.
Название региона должно быть уникальным внутри tenant-организации.

Улицы региона должны быть доступны внутри карточки региона как связанный справочник.
Улица в коде называется Street.
Таблица улиц называется streets.
Основные поля улицы: id, organization_id, region_id, name.
Улица принадлежит tenant-организации и региону.
organization_id улицы должен совпадать с organization_id региона.
Название улицы должно быть уникальным внутри связки tenant-организация + регион.
Абонент выбирает адрес из этого справочника: сначала регион, затем улицу внутри выбранного региона.
В карточке абонента нельзя выбрать улицу из другого региона или другой tenant-организации.

5. Абоненты
В системе должна быть карточка абонента.
Основные поля абонента
ПолеОписаниеЛицевой счётУникальный номер абонентаФИО / НаименованиеИмя физического лица или название организацииТип абонентаФиз. лицо / ИП / ТОО / Коммерческие объекты / БюджетУслугаУслуга выбранной tenant-организации, не выбирается в карточке абонентаТип начисленияПо счётчику / на одного человека / фиксированная суммаКоличество проживающихИспользуется для расчёта на одного человекаФиксированная суммаИспользуется при фиксированном начисленииСтатусАктивный / неактивныйТелефонКонтактный номерРегионРегион из справочника tenant-организацииУлицаУлица выбранного регионаДомНомер домаКвартира / помещениеНомер квартиры или помещенияПримечаниеДополнительная информация

На первом этапе настройки начисления хранятся в таблице clients.
Поля настроек начисления клиента: client_type, billing_type, residents_count, fixed_amount.
Поле residents_count показывается только при billing_type = per_person.
Поле fixed_amount показывается только при billing_type = fixed.
При billing_type = meter дополнительные поля настроек начисления не показываются.
Поле utility_service_id не должно быть пользовательской настройкой абонента.
Если utility_service_id технически хранится в связанных таблицах для снимков или связей, его значение должно соответствовать услуге выбранной tenant-организации.
Доступные значения billing_type: meter, per_person, fixed.
Адрес абонента хранится структурно: region_id, street_id, house, apartment.
Поле region_id обязательно и выбирается из регионов tenant-организации.
Поле street_id обязательно и выбирается только из улиц выбранного региона.
Поля house и apartment необязательные.
Карточка абонента является основным рабочим экраном по лицевому счёту.
Внутри карточки абонента должны отображаться связанные таблицы: счётчики, оплаты, начисления и квитанции.
Показания отображаются внутри карточки конкретного счётчика.

6. Лицевой счёт
Лицевой счёт используется для идентификации абонента в системе.
Правила


лицевой счёт должен быть уникальным внутри организации;


лицевой счёт может вводиться вручную или формироваться автоматически;


по лицевому счёту ведутся начисления, оплаты, сальдо, показания и квитанции;


один абонент имеет один основной лицевой счёт;


префиксы для физлиц и юрлиц не используются.



7. Услуга организации
В системе не должно быть набора услуг внутри tenant-организации.
Каждая tenant-организация создаётся под одну коммунальную услугу: например, водоснабжение или вывоз мусора.
Если нужно вести ещё одну услугу, создаётся отдельная tenant-организация.
Технически значение услуги может храниться через UtilityService.
Название Service не используется для модели, чтобы не путать коммунальную услугу с сервисными классами Laravel.
UtilityService не должен использоваться как список услуг, который оператор выбирает внутри уже созданной организации.
Внутри организации активна только услуга этой организации.
Примеры услуг
УслугаЕдиница измеренияВодоснабжением³ЭлектроэнергиякВт⋅чВывоз мусорамесяц / человек / объектКанализациям³Обслуживаниемесяц
Основные поля услуги организации
ПолеОписаниеОрганизацияTenant-организация, для которой задана услугаНазвание услугиНапример: водоснабжение, электричество, мусорЕдиница измерениям³, кВт⋅ч, месяц, человек, объектСтатусАктивная / неактивнаяПримечаниеДополнительная информация

8. Услуга клиента
У абонента не выбирается отдельная услуга.
Услуга клиента всегда равна услуге выбранной tenant-организации.
Правила


у одного абонента внутри организации может быть только услуга этой организации;


услуга не выбирается в карточке абонента;


услуга используется при начислении, формировании квитанции и отчётов;


если абоненту нужна другая коммунальная услуга, для этой услуги создаётся отдельная tenant-организация и абонент ведётся в ней отдельно;


история начислений сохраняет снимок услуги организации на момент расчёта.



9. Тип начисления
Тип начисления указывается в карточке абонента.
Техническое поле типа начисления в карточке клиента — billing_type.
В системе должны быть доступны следующие типы начисления:
Тип начисленияОписаниеТехническое значениеПо счётчикуНачисление рассчитывается по показаниям счётчикаmeterНа одного человекаНачисление рассчитывается как количество проживающих × тариф на одного человекаper_personФиксированная суммаКаждый месяц начисляется заранее указанная суммаfixed
Правила


тип начисления выбирается отдельно для каждого абонента;


тип начисления не относится к организации;


у одного абонента может быть только один активный тип начисления;


если выбран тип “по счётчику”, у абонента должен быть один или несколько активных счётчиков;


если выбран тип “на одного человека”, используется количество проживающих и тариф для типа абонента;


если выбран тип “фиксированная сумма”, начисляется указанная сумма без счётчика и тарифа.



10. Тарифы
Тариф в коде называется Tariff.
Таблица тарифов называется tariffs.
Если utility_service_id хранится у тарифа, он должен совпадать с услугой выбранной tenant-организации.
Основные технические поля тарифа: organization_id, utility_service_id, client_type, unit_price, per_person_price, starts_on, status.
Тип абонента в коде называется ClientType.
ClientType хранится как enum и используется в карточке клиента и тарифе.
Значения ClientType: Физ. лицо, ИП, ТОО, Коммерческие объекты, Бюджет.
Отдельного справочника категорий тарифов нет.
Система должна хранить тарифы по услуге организации и типам абонентов.
Основные поля тарифа
ПолеОписаниеУслугаУслуга выбранной tenant-организацииТип абонентаФиз. лицо / ИП / ТОО / Коммерческие объекты / БюджетЦена за единицуСтоимость за 1 м³, 1 кВт⋅ч или другую единицу услуги, поле unit_priceЦена на одного человекаСумма начисления на одного проживающего, поле per_person_priceДата начала действияС какого периода действует тарифСтатусАктивный / неактивныйПримечаниеДополнительная информация
Правила


тариф выбирается по услуге организации, типу абонента и периоду;


в расчёте используется тариф, действующий на выбранный период;


для начисления по счётчику используется unit_price;


для начисления на одного человека используется per_person_price;


для фиксированной суммы тариф не используется;


старые тарифы должны сохраняться для истории расчётов.



11. Нормативы
Отдельный справочник нормативов на первом этапе не используется.
Начисление на одного человека считается напрямую по сумме `per_person_price`, указанной в тарифе для типа абонента.
Таблица `normatives`, модель `Normative` и CRUD-раздел нормативов не входят в целевую модель первого этапа.
Если позже понадобится физическая норма потребления, например `количество проживающих × норма × тариф`, это должно быть вынесено в отдельный этап и отдельное изменение ТЗ.



12. Счётчики
Счётчик в коде называется Meter.
Таблица счётчиков называется meters.
Счётчик используется только для абонентов с типом начисления “по счётчику”.
Основные поля счётчика
ПолеОписаниеНомер счётчикаЗаводской или фактический номер счётчикаЛицевой счётАбонентУслугаУслуга выбранной tenant-организацииДата установкиДата установки счётчикаНачальное показаниеПоказание на момент установкиДата снятияДата отправки счётчика в архивСтатусАктивный / в архивеПримечаниеДополнительная информация
Правила


номер счётчика должен быть уникальным внутри tenant-организации;


счётчик относится к tenant-организации и абоненту, а коммунальная услуга берётся из tenant-организации;


у абонента может быть несколько активных счётчиков;


все активные счётчики абонента используются при начислении по счётчику;


дата снятия не редактируется вручную в форме счётчика;


счётчик отправляется в архив действием «Отправить в архив» после подтверждения;


при отправке в архив система проставляет дату снятия текущей датой и переводит статус в removed;


счётчик возвращается в активный учёт действием «Вывести из архива» после подтверждения;


при выводе из архива система очищает дату снятия и переводит статус в active;


начальное показание используется при первом вводе показаний.


показания счётчика отображаются и создаются внутри карточки счётчика как связанная таблица.



13. Показания счётчиков
Показание счётчика в коде называется MeterReading.
Таблица показаний счётчиков называется meter_readings.
Система должна позволять вводить показания по счётчику за каждый расчётный период.
Показания нужны только для абонентов с типом начисления “по счётчику”.
Основные поля показания
ПолеОписаниеЛицевой счётАбонентСчётчикНомер счётчикаПериодРасчётный месяцПредыдущее показаниеПоказание прошлого периодаТекущее показаниеПоказание текущего периодаКорректировкаИсправленное значение показанияДата вводаКогда внесено показаниеКомментарийПричина корректировки или примечание
Правила


за один период по одному счётчику должно быть только одно показание;


ввод показаний выполняется внутри карточки счётчика в связанной таблице показаний;


поле “Предыдущее показание” при создании заполняется системой автоматически;


при первом вводе предыдущее показание берётся из начального показания счётчика;


в следующих месяцах предыдущее показание берётся из прошлого текущего показания;


если показание создаётся за более поздний период, предыдущее показание берётся из последнего текущего показания этого счётчика за период раньше выбранного;


если указана корректировка, в расчёте используется корректированное значение;


расход = текущее показание - предыдущее показание;


если показания за период нет хотя бы по одному активному счётчику абонента, начисление по счётчику за этот период не создаётся, а закрытие месяца показывает ошибку оператору.



14. Оплаты
Оплата в коде называется Payment.
Таблица оплат называется payments.
Система должна учитывать оплаты абонентов.
Оплата привязывается к лицевому счёту.
Основные поля оплаты
ПолеОписаниеЛицевой счётАбонентПериодРасчётный месяцСумма оплатыФактически оплаченная суммаКорректировка оплатыИсправленная сумма оплатыПричина корректировкиПочему изменена суммаДата оплатыДата поступления оплатыДата корректировкиКогда была внесена корректировкаКомментарийДополнительная информация
Правила


оплата привязывается к лицевому счёту;


если указана корректировка оплаты, в расчёте используется корректированная сумма;


оплаты должны отображаться в карточке абонента;


оплаты должны отображаться в квитанции;


по одному абоненту может быть несколько оплат за период.


при закрытии месяца все оплаты абонента за период суммируются.



14.1. Корректировки сальдо
Корректировка сальдо в коде называется BalanceAdjustment.
Таблица корректировок называется balance_adjustments.
Система должна учитывать корректировки сальдо абонентов.
Корректировка привязывается к лицевому счёту и расчётному периоду.
Основные поля корректировки
ПолеОписаниеЛицевой счётАбонентПериодРасчётный месяцТипВходящий остаток / ручная корректировка / списание / перерасчётСуммаСумма корректировкиДата корректировкиДата операцииПричина / примечаниеДополнительная информация
Правила


по одному абоненту может быть несколько корректировок за период;


положительная сумма увеличивает задолженность;


отрицательная сумма уменьшает задолженность или увеличивает переплату;


при закрытии месяца все корректировки абонента за период суммируются;


корректировки должны отображаться в карточке абонента.



15. Расчёт начислений
Расчёт выполняется по каждому абоненту за выбранный период.
Расчётная связка
Лицевой счёт + Период
Услуга берётся из выбранной tenant-организации.
Тип начисления берётся из карточки абонента.

15.1. Расчёт по счётчику
Используется, если у абонента выбран тип начисления “по счётчику”.
Формула расхода
Расход = Текущее показание - Предыдущее показание
Если есть корректировка:
Расход = Корректированное показание - Предыдущее показание
Формула начисления
Начисление = Расход × unit_price

15.2. Расчёт на одного человека
Используется, если у абонента выбран тип начисления “на одного человека”.
Формула начисления
Начисление = Количество проживающих × per_person_price

15.3. Фиксированное начисление
Используется, если у абонента выбран тип начисления “фиксированная сумма”.
Начисление = Фиксированная сумма
При фиксированном начислении счётчик и тариф не используются.
Закрытие месяца рассчитывает всех активных абонентов с billing_type = fixed, per_person и meter.
Для фиксированного начисления нужны услуга выбранной tenant-организации и fixed_amount больше 0.
Для начисления на одного человека используется формула:
residents_count * per_person_price.
Для счётчиков используется суммарный расход показаний за период:
Начисление = сумма consumption по всем активным счётчикам абонента * unit_price.
Для billing_type = meter нужны услуга выбранной tenant-организации, тип абонента, один или несколько активных счётчиков по услуге организации, показание за период по каждому активному счётчику и активный тариф.

16. Сальдо
Система должна рассчитывать начальное и конечное сальдо по лицевому счёту.
Начальное сальдо
Если предыдущего начисления нет, начальное сальдо равно 0.
Для следующих месяцев используется конечное сальдо предыдущего месяца.
Входящие остатки, ручные исправления, списания и перерасчёты вводятся через корректировки сальдо за расчётный период.
Конечное сальдо
Конечное сальдо = Начальное сальдо + Начисление - Оплата + Корректировка
Правила


сальдо ведётся по лицевому счёту;


если оплат за период несколько, они суммируются;


если корректировок за период несколько, они суммируются;


если оплата больше начисления и долга, появляется переплата;


если начисление больше оплаты, появляется задолженность.



17. Закрытие месяца
В системе должна быть функция закрытия расчётного месяца.
При закрытии месяца система фиксирует
ПолеОписаниеЛицевой счётАбонентУслугаУслуга выбранной tenant-организацииПериодРасчётный месяцТип начисленияПо счётчику / на одного человека / фиксированная суммаОбъёмРасход или количество проживающихТарифИспользованная ценаНачислениеСумма начисленияОплатаСумма оплатыКорректировкаСумма корректировокНачальное сальдоСальдо на начало периодаКонечное сальдоСальдо на конец периодаДата закрытияКогда выполнено закрытие
Правила


закрытие выполняется за выбранный период;


закрытый месяц считается зафиксированным;


данные закрытого месяца используются для квитанций;


один абонент не должен иметь два расчёта за один и тот же период;

на первом этапе результат закрытия месяца сохраняется в Accrual;

в Accrual сохраняются начисления по фиксированной сумме, на одного человека и по счётчику;

для начисления на одного человека должен существовать активный тариф для типа абонента с заполненным per_person_price;

для начисления по счётчику должны существовать один или несколько активных счётчиков по услуге организации, показание за период по каждому активному счётчику и активный тариф для типа абонента с заполненным unit_price;

сумма оплат за период сохраняется в Accrual.paid_amount;

сумма корректировок за период сохраняется в Accrual.adjustment_amount;

конечное сальдо считается по формуле: начальное сальдо + начисление - оплаты + корректировки;

повторное закрытие уже рассчитанного периода не создаёт дубль начисления по тому же абоненту;


если данные изменились после закрытия, должен выполняться перерасчёт;


закрытие месяца рассчитывает всех активных абонентов;

если по активному абоненту не хватает данных, начисление не создаётся, а процесс показывает ошибку по лицевому счёту и абоненту.



18. Перерасчёт
Перерасчёт относится ко второму этапу развития системы.
На первом этапе перерасчёт не выполняется автоматически: базовая логика должна стабильно закрывать месяц, создавать начисления, учитывать оплаты и формировать квитанции.
Система должна позволять выполнить перерасчёт за выбранный период.
Основания для перерасчёта


изменились показания;


изменилась сумма оплаты;


изменилась корректировка;


изменился тариф;


изменился тариф на одного человека;


изменилась фиксированная сумма;


был изменён тип начисления;


была обнаружена ошибка в данных.


При перерасчёте система должна


удалить старый расчёт за выбранный период;


удалить старую квитанцию за выбранный период;


заново выполнить расчёт;


сформировать новую квитанцию.


Правила


перерасчёт выполняется по выбранному периоду;


новая квитанция должна заменить старую;


оператор должен видеть, что был выполнен перерасчёт;


история перерасчёта должна быть понятна бухгалтерии.



19. Квитанции
Квитанция в коде называется Receipt.
Таблица квитанций называется receipts.
Система должна формировать квитанцию после закрытия месяца.
Квитанция формируется по одному абоненту за один расчётный период.
Квитанция строится из сохранённого Accrual, а не пересчитывает начисление заново на лету.
Основные поля квитанции
ПолеОписаниеНомер квитанцииУникальный номер квитанцииНачислениеИсточник квитанции, AccrualЛицевой счётАбонентПериодРасчётный месяцФИО / НаименованиеДанные абонентаУслугаУслуга выбранной tenant-организацииТип начисленияПо счётчику / на одного человека / фиксированная суммаРасход / объёмРасход или количество проживающихТарифИспользованная ценаНачисленоСумма начисленияОплаченоСумма оплатыКорректировкаСумма корректировокНачальное сальдоСальдо на начало периодаКонечное сальдоСальдо на конец периодаДата формированияКогда создана квитанция
Правила


квитанция формируется по сохранённому Accrual;

квитанция не должна пересчитывать начисление, оплату или сальдо на лету;


квитанция должна содержать услугу выбранной tenant-организации;

один абонент не должен иметь две квитанции за один период внутри одной tenant-организации;

один Accrual не должен иметь больше одной квитанции;


после формирования квитанция должна быть доступна для просмотра и печати;


при перерасчёте старая квитанция удаляется и создаётся новая;


квитанция должна быть понятна абоненту, оператору и бухгалтерии.



20. Карточка абонента
В карточке абонента должны отображаться:


основные данные абонента;


регион, улица, дом и квартира / помещение;


телефон;


статус;


услуга организации;


тип начисления;


тип абонента;


счётчик, если начисление по счётчику;


история показаний;


история оплат;


история начислений;


текущее сальдо;


квитанции;


история перерасчётов.



21. Раздел “Абоненты”
Функции раздела:


список абонентов;


поиск по лицевому счёту;


поиск по ФИО / наименованию;


поиск по региону, улице, дому или квартире / помещению;


добавление абонента;


редактирование абонента;


изменение статуса;


выбор типа начисления;


открытие карточки абонента.


Карточка абонента должна содержать связанные таблицы:


счётчики абонента;


оплаты абонента;


начисления абонента;


квитанции абонента.



22. Услуга организации
Функции раздела:


отображение услуги выбранной tenant-организации;


выбор услуги при создании tenant-организации;


редактирование услуги организации только до начала учёта или как административное исправление;


создание отдельной tenant-организации для новой коммунальной услуги;


настройка единицы измерения;


запрет на добавление нескольких услуг внутри одной tenant-организации.



23. Раздел “Тарифы”
Функции раздела:


список тарифов;


добавление тарифа;


редактирование тарифа;


выбор типа абонента;


указание цены за единицу и цены на одного человека;


указание даты начала действия;


изменение статуса тарифа.



24. Типы абонентов
Типы абонентов фиксируются enum `ClientType`.
Отдельный CRUD-раздел для типов абонентов не используется.
Тип абонента выбирается в карточке абонента и в тарифе.
Доступные значения: Физ. лицо, ИП, ТОО, Коммерческие объекты, Бюджет.



25. Раздел “Счётчики”
Счётчики абонента должны быть доступны внутри карточки абонента как связанная таблица.
Из связанной таблицы оператор может добавить счётчик, изменить счётчик, добавить показание для выбранного счётчика, отправить счётчик в архив, вывести счётчик из архива и открыть карточку счётчика.
Карточка конкретного счётчика используется для работы с его показаниями, а быстрое действие в карточке абонента позволяет внести новое показание без перехода.
Функции:


просмотр истории счётчика;


просмотр и ввод показаний внутри карточки счётчика;


просмотр активных и архивных счётчиков.



26. Связанная таблица “Показания” в счётчике
Показания должны быть доступны внутри карточки счётчика как связанная таблица.
Основной сценарий оператора — открыть счётчик и внести показание по этому счётчику или добавить показание напрямую из строки счётчика в карточке абонента.
При создании показания поле “Предыдущее показание” уже заполнено системой.
Функции таблицы:


ввод показаний;


просмотр показаний по периоду;


редактирование показаний;


внесение корректировки;


фильтр по периоду;


просмотр истории показаний.



27. Связанная таблица “Оплаты” в абоненте
Оплаты должны быть доступны внутри карточки абонента как связанная таблица.
Основной сценарий оператора — открыть абонента и внести оплату по его лицевому счёту.
Функции таблицы:


ввод оплаты;


просмотр оплат;


редактирование оплаты;


внесение корректировки;


фильтр по периоду;


просмотр истории оплат.



28. Раздел “Закрытие месяца”
Функции раздела:


выбор расчётного периода;


запуск закрытия месяца;


расчёт всех активных абонентов;


отображение количества рассчитанных абонентов;


отображение ошибок;

отображение ошибок по лицевому счёту, абоненту и причине;


просмотр результата расчёта;


формирование квитанций.



29. Раздел “Квитанции”
Квитанции должны быть доступны внутри карточки абонента как связанная таблица.
Ручное создание и редактирование квитанций не используется.
Функции таблицы:


фильтр по периоду;


просмотр квитанции;


печать квитанции;


квитанции создаются после закрытия месяца из сохранённых начислений.


повторное формирование после перерасчёта.



30. Роли пользователей
Администратор
Может:


управлять пользователями;


управлять справочниками;


управлять услугой организации и создавать отдельные организации под разные услуги;


управлять тарифами;


управлять тарифами по типам абонентов;


выполнять закрытие месяца;


выполнять перерасчёт;


просматривать все данные.


Оператор
Может:


добавлять и редактировать абонентов;


выбирать тип начисления абонента;


добавлять счётчики;


вводить показания;


вводить оплаты;


просматривать карточки абонентов;


печатать квитанции.


Бухгалтер
Может:


просматривать начисления;


просматривать оплаты;


просматривать сальдо;


выполнять сверку;


печатать квитанции;


формировать отчёты.



31. Отчёты
Отчёты относятся ко второму этапу развития системы, после стабилизации базового вертикального сценария.
Система должна предоставлять следующие отчёты:
ОтчётОписаниеСписок абонентовВсе абоненты с лицевыми счетамиАбоненты по организацииВсе абоненты выбранной tenant-организации и её услугиАбоненты по типу начисленияПо счётчику, на одного человека, фиксированная суммаПоказания за периодВсе введённые показанияОплаты за периодВсе оплаты за выбранный месяцНачисления за периодНачисления по абонентамДолжникиАбоненты с задолженностьюПереплатаАбоненты с положительным балансомКвитанцииСписок сформированных квитанцийСводный отчётОбщая сумма начислений, оплат и задолженности за период

32. Поиск и фильтры
В системе должны быть доступны фильтры:


по лицевому счёту;


по ФИО / наименованию;


по региону, улице, дому или квартире / помещению;


по периоду;


по статусу абонента;


по типу начисления;


по типу абонента;


по наличию задолженности;


по наличию счётчика.



33. Общие правила


Основной расчётный период — месяц.


Период указывается в формате ГГГГММ.


Основная связка расчёта: лицевой счёт + период.


У каждой tenant-организации может быть только одна активная коммунальная услуга.


У абонента не выбирается отдельная услуга.


Тип начисления хранится в карточке абонента.


Тип начисления не хранится в организации.


У каждого абонента должен быть выбран тип начисления.


По счётчику начисление выполняется на основании показаний.


На одного человека начисление выполняется на основании `residents_count` и `per_person_price` тарифа.


По фиксированной сумме начисление выполняется без счётчика и тарифа.


Все суммы отображаются в тенге.


Закрытый месяц используется для формирования квитанций.


При изменении данных после закрытия выполняется перерасчёт.


Старые тарифы должны сохраняться для истории.


Все действия должны быть понятны оператору и бухгалтерии.



34. Этапы реализации
Первый этап — базовая логика учёта.

Этап 1. Абоненты и справочники


карточка абонента;


лицевой счёт;


услуга tenant-организации;


типы абонентов;


тарифы;


тарифы по типам абонентов.


Этап 2. Настройка начислений


выбор типа начисления у абонента;


настройка начисления по счётчику;


настройка начисления на одного человека;


настройка фиксированной суммы.


Этап 3. Счётчики и показания


добавление счётчиков;


привязка счётчика к абоненту;


ввод показаний;


история показаний;


корректировка показаний.


Этап 4. Оплаты


ввод оплат;


история оплат;


корректировка оплат;


отображение оплат в карточке абонента.


Этап 5. Расчёты


расчёт по счётчику;


расчёт на одного человека;


расчёт фиксированной суммы;


расчёт сальдо;


закрытие месяца.


Этап 6. Квитанции


формирование квитанции из сохранённого начисления;


просмотр квитанции;


печать квитанции.


Второй этап — перерасчёты и отчёты после стабилизации базовой логики.

Этап 7. Перерасчёт


изменение данных после закрытия;


повторный расчёт;


обновление квитанций.


Этап 8. Отчёты


отчёты по начислениям, оплатам и задолженности.



35. Итоговый результат
В результате должна быть разработана система, которая позволяет вести учёт коммунальных услуг:


водоснабжение;


электроэнергия;


вывоз мусора;


канализация;


обслуживание;


другие услуги при необходимости.


Система должна позволять:


регистрировать абонентов;


вести одну услугу в рамках выбранной tenant-организации;


создавать отдельную tenant-организацию для новой коммунальной услуги;


выбирать тип начисления для абонента;


учитывать счётчики;


вводить показания;


вести тарифы по типам абонентов;


вести тарифы;


фиксировать оплаты;


рассчитывать начисления;


рассчитывать сальдо;


закрывать месяц;


выполнять перерасчёты;


формировать и печатать квитанции;


просматривать историю по каждому абоненту.


Система должна быть простой для оператора, понятной для бухгалтерии и гибкой для разных видов коммунальных услуг.
